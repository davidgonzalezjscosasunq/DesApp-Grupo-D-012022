TAG Entrega 2 1.0
---------------------------------------------------------------------
NEW FEATURES:
* Controllers for TradingService functionality, and their tests added.
* Error handlers added.
* Tests that involved requests to binance and BCRA api tested with wiremock to mock response.
* Code refactors (private fields on classes and public getters, endpoints moved to another file, get token from application properties, some variable names updated, query to find traded volume updated).
* Active crypto list (get a list of coin rates with symbol, rate date, price in dollars and price in pesos).

NOTES:
* To do: Add tests for RatesController.
* To do: Search a way to get just the last dollar rate from BCRA api.
